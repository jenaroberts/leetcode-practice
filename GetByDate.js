//on christmas eve it is time to make christmas cookies
function timeForMilkAndCookies(date) {
  return date.getMonth() === 11 && date.getDate() === 24;
}

const timeCookies = (date) => {
  return date.getMonth() === 11 && date.getDate() === 24;
};
